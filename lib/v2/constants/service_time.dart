class ServiceTime {
  /// The number of seconds required to service one item.
  static const int secondsPerItem = 10;

  /// The time required to service one item.
  static const Duration perItem = Duration(seconds: secondsPerItem);
}
